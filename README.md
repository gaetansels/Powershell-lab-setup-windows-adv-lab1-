# Powershell-lab-setup-windows-adv-lab1-
In this lab, I am setting up a server core which will be operated via the CLI and PowerShell connected to a GUI client. 

First, we install Windows Server Core 2025
We will give the server a hostname and install a new forest on the server. 

After that, we will give a hostname to the client.
Give it a static IP address and add it to our domain 

We will also ad RSAT for remote managment of the server. ...
